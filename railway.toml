[build]
buildCommand = "pnpm install && pnpm turbo run build --filter=@chapterai/api^..."

[deploy]
startCommand = "node apps/api/dist/server.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
